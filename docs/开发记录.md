# UniLife OS 开发记录

更新时间：2026-04-25

本文档用于记录重构过程中的已完成内容、当前阶段计划、风险和下一步任务。后续每次开发结束后都应更新本文件，避免项目状态只散落在聊天记录或提交信息里。

## 给接手模型的快速入口

如果你是后续接手本项目的 AI 编程助手，请按下面顺序执行：

1. 打开 `docs/赛事导向重构规划案.md` 第 0 节「给接手 AI 的交接协议」，按顺序做完 0.1 的 5 分钟开工检查。
2. 回到规划案第 5 节「任务卡队列」，找到状态为 `激活` 的那张卡。
3. 读本文档的「当前激活任务卡摘要」「当前已知风险」。
4. 开始任何代码修改前运行 `git status --short`。
5. 只改当前激活任务卡里列出的「允许改动的文件」，不要顺手重构其他地方。
6. 完成后按规划案第 0.3 节的收尾清单 + 本文档末尾的「后续记录模板」更新两个文档。

如果规划案里的任务卡不够清楚或你对下一步依然不确定，按规划案第 0.5 节的兜底流程处理。

---

## 当前阶段

当前激活任务卡：**R5 完成；R6 待队友主导**

R5 完成时间：2026-04-25 02:22，验收 pytest 52/52 通过 + AppTest 完整渲染 0 异常 + API Key 缺失兜底文案核验。

下一张卡：R6 · 初赛材料与决赛演示（由队友主导，Day 4-6，你辅助修 bug、录屏前跑 `python tools/reset_demo.py`）。

3 天 AI 辅助重构窗口已在 Day 0 当晚（2026-04-25 01:15-02:22，约 67 分钟）内完成 R0-R5 共 5 张卡。6 天日历的 Day 1-3 预算释放为缓冲，可用于演示打磨或提前启动 R6。

赛事窗口：今天 2026-04-25 → 2026-05-01，共 6 天。

---

## 当前激活任务卡摘要（R6，队友主导）

R5 已完成，当前代码层重构全部落地。接下来进入 R6（Day 4-6），主要工作由队友执行，你的辅助职责：

1. 回答队友关于 `agent/` / `proactive/` / `ui/` 架构的问题。
2. 修 R4 演示走查（真实浏览器里）发现的 UI bug，优先级最高。
3. 录屏前一天必须 `python tools/reset_demo.py`。
4. 答辩 PPT 的架构 / 创新性两页，参考本文档「当前 6 天进度」和规划案第 1、3、5 节。

**允许改动**：所有文档、bug 修复范围内的代码。

**禁止改动**：R1-R5 规划案里的所有「禁止改动」红线（红线触发立刻回滚）。

---

## 今日变更记录

### 2026-04-25

阶段：R0 文档刷新 → 准备切入 R1

已完成：

- 重写 `docs/赛事导向重构规划案.md` 为 v2：引入「任务卡队列」格式、加入 6 天日历、5 分钟演示脚本、每张卡的时间预算与验收命令。
- 跳过 SQLite 迁移的决策落地（见「决策记录」）。
- 本文档按新规划案格式刷新：移除 R1.1 SQLite 执行说明，调整快速入口、当前阶段、阶段计划总览。

修改文件：

- `docs/赛事导向重构规划案.md`（整体重写）
- `docs/开发记录.md`（本文件）

未改动：

- 未修改业务代码。
- 未修改 Agent 工具逻辑。
- 未修改持久化格式。

下一步：

- 切换到 R1 任务卡，先从 `get_finance()` 的硬编码日期动态化开始。

### 2026-04-25 01:21

任务卡：R1 · 数据源稳定化（mock 动态日期 + 职责切分）
阶段动作：完成 → 阶段切换至 R2

已完成：

- 新增 `modules/seed_data.py`：所有 base_* 模板集中管理。`build_finance_seed` 21 条消费按 offset 0-20 天动态生成；`build_todos_seed` 7 条按 [-2, +6] 范围；`build_exams_seed` 3 条固定 +1/+8/+15；`build_travel_seed` 日期=下一个周六；`build_health_history_seed` 保留原相对 today 语义。
- 新增 `modules/context_builder.py`：`build_context_summary()` 搬出。对 `mock_data.get_*()` 采用函数内延迟导入，打破循环引用。
- 重写 `modules/mock_data.py`：瘦身为聚合层。所有 `get_xxx()` 签名保持不变，内部调用 `seed_data` + `persistence` 合并。`get_alerts` 保留在本文件。顶部 `from modules.context_builder import build_context_summary` re-export，`app.py` 导入路径不变。
- 新增 `tools/reset_demo.py`：`copy.deepcopy(_DEFAULT_DATA)` + `save_user_data` 清空 overrides。提供 `reset()` 函数，R4 侧边栏按钮可直接调用。
- 装依赖：base miniconda 3.13 原本无 streamlit，已 `pip install -r requirements.txt` 装齐（streamlit 1.56.0 / pandas 3.0.2 / plotly 6.7.0 / altair 6.1.0 / python-dotenv）。

修改文件：

- 新增：`modules/seed_data.py`, `modules/context_builder.py`, `tools/reset_demo.py`
- 重写：`modules/mock_data.py`（579 → 约 270 行）
- 未动：`app.py`, `modules/tools.py`, `modules/persistence.py`, `modules/chat_engine.py`, `prompts/system_prompt.py`

验证命令与结果：

- ✅ 动态日期：最新消费 2026-04-25（gap 0 天）；待办 deadline [2026-04-25 ~ 2026-05-01] 全部落在 [-7, +14] 范围；考试 (线性代数 +1, 高数 +8, 物理 +15)；旅行日期 2026-05-02（本周六）。
- ✅ 模块拆分：`seed_data.build_finance_seed` / `context_builder.build_context_summary` / `mock_data.get_finance` 均可导入；`modules.mock_data.build_context_summary` re-export 正常。
- ✅ 重置脚本：人为污染 1 条 extra_transaction + 3 项 health_overrides → `python tools/reset_demo.py` → 两字段均回到空态。
- ✅ streamlit 启动：1 秒内 `/_stcore/health` 返回 ok。

遗留问题：

- 无功能遗留。环境依赖首次安装，后续会在任何 fresh checkout 上遇到同样需求——requirements.txt 已齐全。

下一步：

- R2 任务卡：Agent 工具体系模块化。目标 1261 行 `modules/tools.py` 按域拆到 `agent/tools/*`，保留 facade 兼容。

检查清单：

- [x] git status 已查看（仅 mock_data 改动 + 3 个新文件 + 3 份文档）
- [x] 当前任务卡的验收命令已跑通（4/4）
- [x] 未修改禁区文件
- [x] 本条记录已追加
- [x] 任务卡状态已更新（R1 完成 → R2 激活）

### 2026-04-25 01:42

任务卡：R2 · Agent 工具体系模块化
阶段动作：完成 → 阶段切换至 R3

已完成：

- 新增 `agent/` 目录，拆分 `modules/tools.py` 1261 行到 5 个生活域模块 + 基础设施。
  - `agent/schemas.py`、`agent/executor.py`：空注册表 + `execute_tool` 入口（含未知工具/运行时异常兜底）。
  - `agent/__init__.py`：导入时触发 5 个 tools 模块的 `register(schemas, display_names, registry)`。
  - `agent/tools/_validators.py`：`validate_amount / validate_steps / validate_sleep_hours / validate_exercise_goal / validate_cost_non_negative / validate_non_empty_str`。
  - 域模块：`course.py`（4）、`finance.py`（3）、`health.py`（7）、`todo.py`（4，含 query_exams）、`travel.py`（6，含 update_packing）。总计 24。
- `modules/tools.py` 1261 → 16 行 facade：`from agent import TOOL_SCHEMAS, TOOL_DISPLAY_NAMES, execute_tool`。
- 备份原文件至 `modules/tools.py.bak`；`.gitignore` 追加 `*.bak`，不污染 git status。

修改文件：

- 新增：`agent/__init__.py`, `agent/schemas.py`, `agent/executor.py`, `agent/tools/__init__.py`, `agent/tools/_validators.py`, `agent/tools/course.py`, `agent/tools/finance.py`, `agent/tools/health.py`, `agent/tools/todo.py`, `agent/tools/travel.py`
- 重写：`modules/tools.py`（1261 → 16 行 facade）
- 改动：`.gitignore`（追加 `*.bak`）
- 未动：`app.py`, `modules/chat_engine.py`, `modules/persistence.py`, `modules/mock_data.py`, `prompts/system_prompt.py`, `agent/**` 的单个工具实现不依赖 `agent` 之外的其他变动

验证命令与结果：

- ✅ 工具数量：`len(TOOL_SCHEMAS) == 24` 通过；schema 和 display_names 的 key 完全对齐。
- ✅ 查询类 6 个：`query_schedule / query_finance / query_health / query_todos(pending) / query_exams / query_travel` 全部返回非空文本，无错误。
- ✅ 写入类 + 校验兜底：`add_todo` / `record_expense` 正常写入；空项目、负金额、越界步数、未知工具名均返回友好错误。
- ✅ streamlit 启动：1 秒内 `/_stcore/health` 返回 ok。

遗留问题：

- 无功能遗留。`modules/tools.py.bak` 作为红线回滚材料保留，`.gitignore` 已跳过。

下一步：

- R3 任务卡：主动关怀引擎。目标新增 `proactive/` 三件套（events/rules/engine）+ 12h 去重 + Agent 通过 `alert_summary` 看前 5 条未读事件。

检查清单：

- [x] git status 已查看（agent/ 新增 + modules/tools.py 重写 + .gitignore）
- [x] 当前任务卡的验收命令已跑通（4/4）
- [x] 未修改禁区文件
- [x] 本条记录已追加
- [x] 任务卡状态已更新（R2 完成 → R3 激活）

### 2026-04-25 01:57

任务卡：R3 · 主动关怀引擎
阶段动作：完成 → 阶段切换至 R4

已完成：

- 新增 `proactive/` 三件套（含 `__init__.py`）：
  - `events.py`：`ProactiveEvent` dataclass + `list_all/list_unread/upsert/mark_read` 薄封装。
  - `rules.py`：6 条 `check_*` 规则，均返回 `list[ProactiveEvent]`，支持同规则多事件（如多个待办到期）。
  - `engine.py`：`scan_and_persist`（12h dedupe）+ `scan_safely`（吞异常）+ `list_unread`。
- `modules/persistence.py`：`_DEFAULT_DATA` 追加 `proactive_events`、`proactive_read_keys`；新增 `list_proactive_events / upsert_proactive_event / mark_proactive_read / get_proactive_read_keys`（约 45 行）。
- `modules/mock_data.py`：`get_alerts()` 重写为 engine 薄包装 —— 触发扫描 → 读取 `list_unread(limit=5)` → 映射为 alert dict（保留 `severity/icon/title/message`，新增 `suggested_action/dedupe_key`）。
- `modules/context_builder.py`：`alert_summary` 加引导文案（「系统主动扫描，如与话题相关请自然带到」），并在每条后附 `建议动作`。
- `prompts/system_prompt.py`：`### 🔔 当前提醒` 改为 `### 🔔 主动关怀事件（系统主动扫描，12h 去重）`。
- `agent/executor.py`：`execute_tool` 成功路径末尾调 `proactive.engine.scan_safely()`（延迟导入 + 吞异常，不影响主流程）。

规则覆盖：`budget_risk` / `todo_due:{id}` / `exam_near:{course}` / `sleep_short` / `exercise_missing` / `travel_packing:{trip}`。12h 去重 key 以 `dedupe_key` 为准。

修改文件：

- 新增：`proactive/__init__.py`, `proactive/events.py`, `proactive/rules.py`, `proactive/engine.py`
- 改动：`modules/persistence.py`（+45 行 proactive CRUD）、`modules/mock_data.py`（`get_alerts` 重写）、`modules/context_builder.py`（alert_summary 强化）、`prompts/system_prompt.py`（标题改名）、`agent/executor.py`（收尾钩子）
- 未动：`app.py`（R4 接入 UI）、`modules/chat_engine.py`、`modules/tools.py`、`agent/tools/**`

验证命令与结果：

- ✅ 6 条 `check_*` 规则全部存在且返回 list。
- ✅ 首轮扫描命中 7 条事件（budget_risk + 4 todo_due + exam_near 线性代数 + exercise_missing）；第二轮 12h 去重返回 []；`mark_read` 生效。
- ✅ `get_alerts()` 返回的每条 alert 都含 `severity/icon/title/message` + 新 `suggested_action/dedupe_key`。
- ✅ streamlit 启动 1 秒内 `/_stcore/health` 返回 ok，根路径 HTTP 200，日志无错误。

遗留问题：

- R4 需要把「主动关怀事件」结构化卡片放到 UI 顶部，并接「已读」按钮到 `mark_read`。
- 规则数据源依赖 mock dynamic date；今天早上 01:xx 跑得到的 7 条事件依赖今天是 2026-04-25、月末前 5 天、上半夜无运动打卡。

下一步：

- R4 任务卡：拆 `app.py` 为 `ui/*`，接入 proactive 卡片 + 演示重置按钮，走一遍 5 分钟演示脚本。

检查清单：

- [x] git status 已查看（proactive/ 新增 + 4 份业务文件改动 + agent/executor 挂钩）
- [x] 当前任务卡的验收命令已跑通（4/4）
- [x] 未修改禁区文件（app.py / chat_engine.py / tools.py / agent/tools/** 均无改动）
- [x] 本条记录已追加
- [x] 任务卡状态已更新（R3 完成 → R4 激活）

### 2026-04-25 02:10

任务卡：R4 · UI 拆分与演示体验打磨
阶段动作：完成 → 阶段切换至 R5

已完成：

- 新增 `ui/` 目录共 5 个视图模块（873 行），把 app.py 735 行压缩到 49 行。
  - `ui/styles.py`：CSS + PWA meta，一次性注入。
  - `ui/components.py`：toast_and_rerun / apply_pending_toast / alert_card_html / travel_item_html / render_header / render_alerts / generate_welcome。
  - `ui/sidebar.py`：侧边栏完整逻辑，拆成 _api_status / _today_schedule / _finance / _health / _todo / _exam / _clear_chat / _demo_reset 8 个子块。
  - `ui/chat.py`：AI 对话 Tab（保留原 st.container + st.chat_input 在容器外的布局，避免 Phase 7 rerun 循环）。
  - `ui/dashboard.py`：数据看板（饼图/课表/健康趋势/旅行）。
- **主动关怀卡片升级**：`render_alerts` 改读 `proactive.engine.list_unread(limit=5)` → 顶部 3 张卡片（high/medium/low 红/橙/绿）+ 剩余折叠。每张卡片含 reason + 建议动作 caption + 「标为已读」按钮（调 `proactive.mark_read`）。
- **演示重置按钮**：侧边栏底部仅在 `APP_MODE=demo` 环境变量下显示「🧹 一键重置 demo 数据」，调用 `tools.reset_demo.reset()` + rerun。
- `app.py.bak` 备份原 735 行入口（`.gitignore` 已跳过 `*.bak`）。

修改文件：

- 新增：`ui/__init__.py`、`ui/styles.py`（140）、`ui/components.py`（159）、`ui/sidebar.py`（273）、`ui/chat.py`（101）、`ui/dashboard.py`（191）
- 重写：`app.py`（735 → 49 行）
- 未动：`agent/**`、`proactive/**`、`modules/**`（除 mock_data 之前 R3 的改动）、`prompts/**`

验证命令与结果：

- ✅ `wc -l app.py` = 49（远低于 < 400 上限）。
- ✅ `import ui.sidebar / ui.chat / ui.dashboard / ui.components / ui.styles` 全部成功，关键符号齐全。
- ✅ `streamlit run app.py` 1 秒内 `/_stcore/health` ok；根路径 200；再访问 200；日志无 Python 异常。
- ✅ `streamlit.testing.v1.AppTest.from_file("app.py").run()` 完整渲染无 exception：sidebar 15 个 markdown 块，主页 44 个，2 个 tabs，「主动关怀事件」标题存在，demo 模式 1 个重置按钮，5 个「标为已读」按钮（3 顶部 + 2 折叠）。

遗留问题：

- 人工演示走查（规划案第 6 节 7 步脚本）需要真实浏览器交互，无人值守阶段无法完成，延后到 Day 4 队友介入时做。
- Streamlit deprecation warning：`use_container_width` 将于 2025-12-31 废弃，改为 `width='stretch'`。非阻塞，R5 或 R6 再清理。

下一步：

- R5 任务卡：pytest 覆盖核心链路 + 异常兜底 + 可观测性折叠区。

检查清单：

- [x] git status 已查看（app.py + ui/ + agent/ + proactive/ + modules/context_builder + modules/seed_data + tools/）
- [x] 当前任务卡的验收命令已跑通（4/4 + AppTest 完整渲染）
- [x] 未修改禁区文件（agent/tools 未动）
- [x] 本条记录已追加
- [x] 任务卡状态已更新（R4 完成 → R5 激活）

### 2026-04-25 02:22

任务卡：R5 · 测试、鲁棒性与可观测性
阶段动作：完成 → R5 结束，切到 R6（队友主导）

已完成：

- pytest 基础设施：`pytest.ini`（testpaths=tests）+ `tests/conftest.py`（`tmp_data_file` fixture：monkeypatch `persistence.DATA_DIR`/`DATA_FILE` 到 tmp_path 并初始化 _DEFAULT_DATA）。`requirements.txt` 追加 `pytest>=8.0.0`。
- 4 个测试文件共 52 项用例：
  - `tests/test_dates.py`（21）：5 个临界 today（月中/月初/月末/周一/周六）下 finance_seed 21 条消费、todos 7 条 deadline、exams 3 场 +1/+8/+15、travel 下一个周六（含今日=周六推到下周的独立用例）。
  - `tests/test_persistence.py`（9）：默认结构、add_expense、todo 状态、水杯跨天重置、运动跨周重置、同日不重复打卡、todo 唯一 ID、proactive upsert/mark_read。
  - `tests/test_tools.py`（14）：24 工具数量、未知工具、非 dict 参数、消费空/负/越界、步数/睡眠/运动目标边界、toggle 未知、6 个查询非空、update_packing、add/delete_course、create travel + add_stop。
  - `tests/test_proactive.py`（8）：6 条规则存在并返回 list[ProactiveEvent]、12h 去重、mark_read、13h 后去重过期、get_alerts 字段兼容、5 条上限、单规则抛错不影响其他。
- 异常兜底：
  - `agent/executor.py`：新增 `_RECENT_TOOL_CALLS`（module-level，最近 5 条）+ `recent_tool_calls()` 导出。未知工具、非 dict 参数、运行时异常都会 `_record_call(ok=False)`。
  - `modules/chat_engine.py`：`chat_agent` 入口加 API Key 缺失的早退出（返回友好错误串，不走 OpenAI client）。
- 可观测性：`ui/sidebar.py` 新增 `_dev_info_block`（`APP_MODE=demo` 时显示）。展示最近 5 次工具调用 + 最近 3 条主动事件（来自 `agent.executor.recent_tool_calls` 和 `proactive.events.list_all`）。

修改文件：

- 新增：`pytest.ini`、`tests/conftest.py`、`tests/test_dates.py`、`tests/test_persistence.py`、`tests/test_tools.py`、`tests/test_proactive.py`
- 改动：`agent/executor.py`（+40 行 observability）、`modules/chat_engine.py`（+8 行 API Key 兜底）、`ui/sidebar.py`（+37 行 dev info block）、`requirements.txt`（+1 行 pytest）
- 未动：`app.py` 宏观布局、`ui/chat.py`、`ui/dashboard.py`、`ui/components.py`、`ui/styles.py`

验证命令与结果：

- ✅ `python -m pytest tests/ -v`：52 项 0.15 秒跑完，全绿。
- ✅ API Key 缺失兜底：`DEEPSEEK_API_KEY=""` 时 `chat_agent` 返回带引导文案的字符串，不抛异常。
- ✅ `APP_MODE=demo` 下 AppTest 完整渲染无异常：sidebar 出现「🧪 开发者信息（observability）」expander 和「🧹 一键重置 demo 数据」按钮。

遗留问题：

- Streamlit `use_container_width` deprecation warning（2025-12-31 后会移除）。可在 R6 演示前做一轮替换：`use_container_width=True` → `width='stretch'`。非阻塞。
- 人工演示走查（规划案第 6 节 7 步）仍需 Day 4 队友介入时做。

下一步：

- R6 由队友主导。你辅助：架构答疑、bug 修复、录屏前 `python tools/reset_demo.py`。
- 赛事窗口 Day 1-3 AI 辅助重构预算（22h）实际在 Day 0 当晚 67 分钟内完成 R0-R5，释放 Day 1-3 作为演示打磨 / 备料缓冲。

检查清单：

- [x] git status 已查看（11 项改动 + 7 个新目录/文件）
- [x] 当前任务卡的验收命令已跑通（pytest 52/52 + AppTest 完整渲染 + API Key 兜底）
- [x] 未修改禁区文件（`app.py` 宏观布局 / `ui/chat.py` / `ui/dashboard.py` / `ui/components.py` / `ui/styles.py` 保持稳定）
- [x] 本条记录已追加
- [x] 任务卡状态已更新（R5 完成 → R6 排队中，由队友激活）

### 2026-04-24

已完成：

- 新增 `docs/赛事导向重构规划案.md`（v1，后被 v2 替换）。
- 新增 `docs/开发记录.md`。
- 更新 `CLAUDE.md` 加入当前重构文档入口。

未改动：

- 未修改业务代码。
- 未修改 Agent 工具逻辑。
- 未修改持久化格式。

---

## 阶段计划总览

| 阶段 | 名称 | 状态 | 时间预算 | 目标 |
|------|------|------|----------|------|
| R0 | 赛事对齐与文档定稿 | 完成 | — | 确认叙事、重构路线和任务卡交接机制 |
| R1 | 数据源稳定化 | 完成 | 4h | mock 动态日期 + 模块职责切分 + 演示重置脚本 |
| R2 | Agent 工具体系模块化 | 完成 | 4h | 拆分 24 个工具到 `agent/tools/*`，保留 facade 兼容 |
| R3 | 主动关怀引擎 | 完成 | 6h | 事件结构 + 6 条规则 + 去重节流 + Agent 感知 |
| R4 | UI 拆分与演示体验 | 完成 | 4h | `app.py` 瘦身到 ui/*；演示链路走查 |
| R5 | 测试、鲁棒性与可观测性 | 完成 | 4h | pytest 覆盖 + 异常兜底 + 开发者信息面板 |
| R6 | 初赛材料与决赛演示 | 排队 | Day 4-6 | 队友执笔，你辅助修 bug 和录屏 |

3 天 AI 重构总预算约 22 小时。超预算先停下来汇报，不要硬怼。

---

## 当前已知风险

1. **mock 日期过期**（R1 即将修复）。  
   当前 mock 里写死 `2026-02-xx`、`2026-03-xx`，今天 2026-04-25 打开应用已经露馅。

2. **大文件耦合**（R2/R4 处理）。  
   `app.py` 735 行，`modules/tools.py` 1261 行。后续改动容易扩大影响面。

3. **缺少自动化测试**（R5 处理）。  
   当前只能做语法检查，无法证明日期、预算、工具异常、持久化读写等核心逻辑稳定。

4. **Streamlit rerun 循环**（R4 注意）。  
   Phase 7 曾尝试悬浮输入框导致颤动回退，本轮不要动宏观布局。

5. **比赛材料需要同步更新**（R6 处理）。  
   如果代码完成重构但技术方案仍描述旧的 mock 增量覆盖架构，会削弱答辩可信度。

---

## 决策记录

### 2026-04-24：不做推倒重写

保留 Streamlit + DeepSeek function calling 的核心技术路线，进行垂直重构。

原因：UI 和 Agent 链路已可用；换栈会吃掉主要开发时间；赛事评分更看重可演示、可解释、可落地的 Agent 能力。

### 2026-04-24：mock 降级为 seed

mock 不再作为事实数据源，后续仅作为 demo 数据初始化来源。

原因：真实持久化状态更适合演示；Agent 工具调用后的状态变化必须真实影响 UI 和后续上下文。

### 2026-04-24：主动关怀采用引擎化设计

先做 App 打开、工具执行、聊天前触发的同步扫描机制，不急于做后台心跳。

原因：没有推送通道时后台心跳收益有限；比赛演示更需要可解释的状态风险事件、去重策略和今日快报。

### 2026-04-25：跳过 SQLite 迁移（本轮）

本轮重构不引入 SQLite。mock 降级为 seed 的目标通过「mock 动态日期 + 演示重置脚本」实现，不走数据库迁移路径。

原因：

- 3 天 AI 辅助重构窗口太紧，9 表 schema + repository 层 + 迁移 + 业务适配至少要吃掉 10+ 小时。
- 赛事评分更看重主动关怀引擎、演示链路和鲁棒性，这些比 SQLite 本身更值得投入时间。
- JSON 增量覆盖已在多轮迭代中稳定，继续沿用风险最低。
- 演示场景里「真实持久化」通过 `data/user_data.json` 原子写入 + 动态日期 + 演示重置脚本已有说服力。

后续如果决赛前有额外时间窗口再补 SQLite 作为演进亮点。

### 2026-04-25：保持 R1→R2→R3→R4→R5 顺序

虽然 R3 主动关怀引擎是演示差异化爆点，仍按原顺序推进。

原因：

- R2 工具模块化先完成能给 R3 提供更干净的 executor 挂钩点（工具执行后扫描）。
- R1 动态日期不修，R3 的 "todo_due" / "exam_near" 等规则会在过期数据上误触发，demo 不好看。
- R4 UI 依赖 R3 的事件结构来呈现「主动关怀卡片」。

### 2026-04-25：规划案改为任务卡队列格式

接手 AI 不再需要从头读整篇规划，直接认领当前激活卡即可。同一时间只允许一张卡激活，避免跨阶段并行修改。

---

## 后续记录模板

每次开发结束后按下面格式追加：

```markdown
### YYYY-MM-DD HH:MM

任务卡：R{N} {名称}
阶段动作：开始 / 推进 / 完成 / 阶段切换 / 红线事件

已完成：
- 

修改文件：
- 

验证命令与结果：
- 

遗留问题：
- 

下一步：
- 

检查清单：
- [ ] git status 已查看
- [ ] 当前任务卡的验收命令已跑通
- [ ] 未修改禁区文件
- [ ] 本条记录已追加
- [ ] 任务卡状态已更新（如切换）
```
